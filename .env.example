# ================================================
# AI Portfolio - Environment Configuration
# ================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ================================================

# ------------------------------------------------
# Server Configuration
# ------------------------------------------------
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# ------------------------------------------------
# Google Cloud Platform
# ------------------------------------------------
# Your GCP project ID (find in GCP Console)
GCP_PROJECT_ID=your-gcp-project-id

# Path to your service account JSON key file
# 1. Go to GCP Console > IAM & Admin > Service Accounts
# 2. Create a service account with "Vertex AI User" role
# 3. Create a key (JSON) and download it
# 4. Put the file in /server/keys/ folder
# 5. Update the path below
GOOGLE_APPLICATION_CREDENTIALS=./server/keys/service-account.json

# Vertex AI region
GCP_LOCATION=us-central1

# AI Models (these defaults work well)
VERTEX_AI_MODEL=gemini-1.5-flash
VERTEX_AI_EMBEDDING_MODEL=text-embedding-004

# ------------------------------------------------
# Firebase (Optional - for data storage)
# ------------------------------------------------
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_SERVICE_ACCOUNT_PATH=./server/keys/service-account.json

# ------------------------------------------------
# Client-side Firebase (Optional)
# ------------------------------------------------
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# ------------------------------------------------
# API Configuration
# ------------------------------------------------
VITE_API_URL=http://localhost:3001
